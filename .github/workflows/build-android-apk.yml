name: Build Android APK

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Allows manual trigger from GitHub UI

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '11'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Install Cordova
      run: |
        npm install -g cordova@11.1.0
        cordova --version

    - name: Install dependencies
      working-directory: ./WaterTracker
      run: |
        npm install

    - name: Add Android platform
      working-directory: ./WaterTracker
      run: |
        cordova platform add android@12.0.1

    - name: Build Debug APK
      working-directory: ./WaterTracker
      run: |
        cordova build android --debug

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: water-tracker-debug-apk
        path: WaterTracker/platforms/android/app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30

    - name: Build Release APK (unsigned)
      working-directory: ./WaterTracker
      run: |
        cordova build android --release
      continue-on-error: true

    - name: Upload Release APK (if built)
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: water-tracker-release-apk-unsigned
        path: WaterTracker/platforms/android/app/build/outputs/apk/release/app-release-unsigned.apk
        retention-days: 30

    - name: Display build info
      run: |
        echo "✅ Build completed successfully!"
        echo "📦 APK files have been uploaded as artifacts"
        echo "🔍 You can download them from the Actions tab"
